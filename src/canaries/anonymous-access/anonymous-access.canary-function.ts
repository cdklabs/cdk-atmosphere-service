// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for AnonymousAccessCanaryFunction
 */
export interface AnonymousAccessCanaryFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/canaries/anonymous-access/anonymous-access.canary.
 */
export class AnonymousAccessCanaryFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: AnonymousAccessCanaryFunctionProps) {
    super(scope, id, {
      description: 'src/canaries/anonymous-access/anonymous-access.canary.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/canaries/anonymous-access/anonymous-access.canary.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}